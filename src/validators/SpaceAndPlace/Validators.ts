import { fieldRequired } from "../commonValidators";

// Physical construction cost
export const constructionCostValidator = fieldRequired;

// Physical lease cost
export const leaseCostValidator = fieldRequired;

// Physical lease size
export const leaseSizeValidator = fieldRequired;

// Lease length months
export const leaseLengthMonthsValidator = fieldRequired;

// Lease length years
export const leaseLengthYearsValidator = fieldRequired;
