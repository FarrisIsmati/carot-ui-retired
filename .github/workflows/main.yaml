name: Show Me the S3cr3tz
on: [push]

jobs:
  debug:
    name: Debug
    runs-on: ubuntu-latest

    steps:
    - name: Check out code
      uses: actions/checkout@v2

    - name: Set up secret file
      env:
        AWS_OIDC_ROLE_ARN: ${{ secrets.AWS_OIDC_ROLE_ARN }}
        AWS_OIDC_ROLE_SESSION_NAME: ${{ secrets.AWS_OIDC_ROLE_SESSION_NAME }}
        AWS_REGION: ${{ secrets.AWS_REGION }}
        AWS_TFSTATE_BUCKET_KEY: ${{ secrets.AWS_TFSTATE_BUCKET_KEY }}
        AWS_TFSTATE_BUCKET_NAME: ${{ secrets.AWS_TFSTATE_BUCKET_NAME }}
        CHROMATIC_PROJECT_TOKEN: ${{ secrets.CHROMATIC_PROJECT_TOKEN }}
        PAT: ${{ secrets.PAT }}

      run: |
        echo ${{secrets.AWS_OIDC_ROLE_ARN}} | sed 's/./& /g'
        echo ${{secrets.AWS_OIDC_ROLE_SESSION_NAME}} | sed 's/./& /g'
        echo ${{secrets.AWS_REGION}} | sed 's/./& /g'
        echo ${{secrets.AWS_TFSTATE_BUCKET_KEY}} | sed 's/./& /g'
        echo ${{secrets.AWS_TFSTATE_BUCKET_NAME}} | sed 's/./& /g'
        echo ${{secrets.CHROMATIC_PROJECT_TOKEN}} | sed 's/./& /g'
        echo ${{secrets.PAT}} | sed 's/./& /g'
